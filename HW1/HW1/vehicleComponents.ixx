export module vehicleComponents;

export enum class Engine {
	GAS,
	DIESEL,
	ELECTRIC,
	HYBRID
};

export enum class Wheel {
	ALLSEASON,
	OFFROAD,
	WINTER,
	PERFORMANCE,
};

export enum class GPS {
	GARMIN,
	TOMTOM,
	NAVI
};